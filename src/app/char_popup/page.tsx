import CharPopup from '@/components/char-popup/char-popup';

export default function CharPopupPage() {
    return (
        <div className="min-h-screen bg-gradient-to-r from-white to-white transition-all duration-500">
            <CharPopup />
        </div>
    );
}
